security:
  oauth2:
    client:
      clientId: git
      clientSecret: secret
      tokenInfoUri: 'http://localhost:8080/oauth/token?client_id=%s&client_secret=%s&code=%s&grant_type=authorization_code'

spring:
  profiles:
    active: pro

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    org.springframework.security.oauth2: DEBUG
    org.springframework.boot.autoconfigure: DEBUG
    cn.flizi: INFO
